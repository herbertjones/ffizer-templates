(defsystem "{{ project }}.test"
  :name "{{ project }}.test"
  :version "0.0.1"

  :class :package-inferred-system
  :pathname "tests/{{ project }}.test"

  :depends-on ("{{ project }}"
               "parachute"
               "{{ project }}.test/module")
  :perform (asdf:test-op (op c)
                         (uiop:symbol-call :parachute :test
                                           :{{ project }}.test)))
