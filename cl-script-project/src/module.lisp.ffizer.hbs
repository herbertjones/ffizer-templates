(uiop:define-package #:{{ project }}
                     (:nicknames #:{{ project }}/src/module)
                     (:use)
                     (:mix #:cl)
                     (:export)
                     (:documentation "Top level file for {{ project }}."))
(cl:in-package #:{{ project }})
