list:
    just --list

install-deps:
    peru sync
    ocicl install
    ./clean-systems

update-deps:
    peru reup
    ocicl latest
    ./clean-systems

build:
    ./build

clean:
    rm ./{{ project }}
    ln -s ./{{ project }}-i ./{{ project }}

test:
    ./test
